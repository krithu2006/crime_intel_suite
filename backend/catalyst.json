{
  "appsail": [
    {
      "source": ".",
      "name": "crime-intel-api"
    }
  ],
  "client": {
    "source": "client",
    "scripts": {
      "predeploy": "npm install --prefix ../frontend && npm run build --prefix ../frontend && node -e \"const fs=require('fs');fs.rmSync('client',{recursive:true,force:true});fs.cpSync('../frontend/dist','client',{recursive:true});fs.copyFileSync('../frontend/package.json','client/package.json');\""
    }
  }
}