fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
networkx>=3.2.0
requests>=2.31.0
# Optional but recommended — risk_scoring.py and prediction.py use these when
# available and fall back to scikit-learn / a feature-importance heuristic
# otherwise, so the app still runs without them.
xgboost>=2.0.0
shap>=0.44.0
